import React from 'react'
import { CenterContainer, UploadCard } from '../../components'

const Upload: React.FC = () => {
	return (
		<CenterContainer>
			<UploadCard />
		</CenterContainer>
	)
}

export default Upload
